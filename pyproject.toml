[project]
name = "kafka-backend"
version = "0.1.0"
description = "This is a fastapi project which is responsible for backend operations of my-kafka-learnings project."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.116.2",
    "kafka-python>=2.2.15",
    "prometheus-client>=0.23.1",
    "uvicorn>=0.36.0",
]
